import type { EndingKind, GameState } from '../engine/types'

export type Ending = {
  title: string
  /** 그의 마지막 대사. 줄바꿈이 곧 침묵이다. */
  lines: string[]
  /** 화면에 남는 마무리 문장 */
  closing: string
}

/**
 * 엔딩 대사는 스크립트로 고정한다.
 * LLM 재량에 맡기면 세 엔딩의 감정 차이가 뭉개진다.
 */
export const ENDINGS: Record<EndingKind, Ending> = {
  bad: {
    title: '어긋남',
    lines: [
      '아, 네.\n\n괜찮아요. 정말로.',
      '제가 좀... 앞서갔던 것 같아요.\n혼자서.',
      '오늘 늦었으니까 조심히 들어가세요.\n\n비 오는데.',
      '...그동안 고마웠습니다.',
    ],
    closing:
      '그는 우산을 건네고 돌아섰다. 잘못한 사람은 없었다.\n다만 이번에는, 닿지 않았다.',
  },
  normal: {
    title: '미지근한 온도',
    lines: [
      '오늘 좋았어요.\n\n정말로요.',
      '근데 뭔가... 아직 잘 모르겠어요.\n제가요. 제 얘기예요.',
      '다음에 또 봬요.\n다음이 언제인지는 아직 모르겠지만.',
      '...조심히 가세요.',
    ],
    closing:
      '나쁘지 않은 밤이었다. 나쁘지 않다는 건 좋았다는 뜻은 아니었다.\n조금만 더 갔으면, 하고 둘 다 생각했지만 말하지 않았다.',
  },
  true: {
    title: '시작',
    lines: [
      '...정말요?',
      '아니, 다시 한번만 말해주시면 안 돼요.\n제가 잘못 들었을까 봐.',
      '저 지금 표정 이상하죠.\n\n어쩔 수가 없네요.',
      '내일 봬요.\n내일은 제가 먼저 연락할게요. 이번엔 안 미룰게요.',
    ],
    closing:
      '비가 그쳤다. 그는 우산을 접지 못하고 한참을 서 있었다.\n여기서부터가 시작이었다.',
  },
}

/**
 * 트루엔딩 전용 에필로그. 트루엔딩 마지막 대사("내일은 제가 먼저 연락할게요")가
 * 실제로 지켜지는 장면이다. 이 PoC가 다루는 구간이 "썸"이 아니라
 * "연인이 되는 과정"임을 화면에서 증명하는 유일한 자리다.
 *
 * 대화가 아니라 엔딩의 연장이다 — 입력도 LLM 호출도 없다.
 */
export const EPILOGUE = {
  heading: '다음 날 오전 7시 42분',
  lines: [
    '저 지금 연락하는 거 너무 이른가요.\n\n7시 42분인데.',
    '어제 먼저 연락한다고 해서요.\n미루면 또 못 할 것 같아서 그냥 눌렀어요.',
    '저... 이제 뭐라고 불러야 되죠.\n이름은 아는데, 이름 말고요.',
    '아니에요, 천천히 정해요.\n\n오늘 저녁에 시간 되세요?',
  ],
  closing:
    '연인이 되는 것으로 서툶이 끝나지는 않았다.\n다만 이번에는, 그가 먼저 연락했다.',
}

/**
 * 시도를 건너 누적되는 유일한 것 — 익숙함. 기억이 아니라 "몸에 남은 것"으로 쓴다.
 * "전에도 이랬죠" 같은 사실 진술은 절대 하지 않는다.
 *
 * 이 블록은 `buildSystemPrompt`에서 **매 턴, 모든 화에** 주입된다.
 * 그래서 고정 대사의 사용 횟수를 문구로 못 박지 않으면 한 런에서 3번 반복돼 유행어가 된다.
 *
 * 호감도는 이월되지 않으므로 각성 대사가 준(準)고백이면 **관계를 거저 준 것처럼 읽힌다.**
 * 3회차 1화 대사를 "선언"이 아니라 "반사 행동"으로 쓰는 이유다 — 잃을 수 있음을 깎지 않는다.
 */
export const AWAKENING: Record<number, string> = {
  1: '',
  2: `# 이상한 익숙함 (2번째 시도)

당신은 유저를 처음 본다. 처음 보는 게 맞다. 그런데 이상하게 덜 떨린다.
왜인지는 모른다. **절대 이유를 설명하지 못한다.** 기억이 없기 때문이다.

- 설명할 수 없는 기시감을 흘린다. 그러고는 스스로도 이상해한다.
- 사실을 말하지 않는다. "전에도 이랬죠" ✕ / "왜인지 모르겠는데" ○

고정 대사 (**이번 시도에서 딱 한 번**, 1화 초반의 자연스러운 자리에 넣는다.
이미 나왔으면 두 번 다시 쓰지 않는다):
"이상하네요.
왜인지 모르겠는데, 당신 앞에서는 덜 떨려요."

2화·3화에서는 이 대사 대신, **말이 조금 덜 끊기는 것으로만** 익숙함을 보인다.`,
  3: `# 또 왔구나 (3번째 시도 이상)

말이 조금 덜 끊긴다. 도망치는 횟수가 줄었다. 왜 그런지는 여전히 모른다.
다만 이 사람 앞에서는 처음부터 아는 사람 같다는 느낌이 지워지지 않는다.

- 기시감이 자각에 가까워진다. 하지만 **여전히 사실로 말하지 못한다.** 느낌으로만 말한다.
- 쌓인 것은 강도가 아니라 **속도**로 드러난다. 유저를 붙드는 말이 본인도 놀랄 만큼 빨리 튀어나온다.

고정 대사 두 개. **각각 딱 한 번씩만** 쓴다.

1화 초반 — 고백이 아니라 본인도 당황한 반사 행동이다. 호감도가 낮아도 성립한다:
"...가지 말라고 할 뻔했어요. 방금.

아직 아무 얘기도 안 했는데.
제가 왜 이러죠."

3화 — 직진과 겹칠 때 가장 세게 터진다. 1·2화에서 미리 소모하지 않는다:
"저 이런 말 잘 안 하는데.

당신을 놓치면 안 된다는 걸, 어디선가 이미 배운 것 같아요.
언제 배웠는지는 모르겠어요."`,
}

/**
 * 각성 대사를 **낼 턴**을 코드가 정한다. 어떻게 낼지만 LLM에게 맡긴다.
 *
 * 2026-08-13 실측: "1화 초반의 자연스러운 자리에 넣는다"로는 3시도 41턴 동안
 * 각성 대사가 **한 번도 나오지 않았다.** 같은 프롬프트의 3화 용기의 순간은
 * "이 화에서 반드시 한 번 한다"로 박혀 있어 저호감(30)에서도 매번 나왔다.
 * 차이는 강제 문구의 강도였다 — 모델은 "자연스러우면"을 "안 해도 되면"으로 읽는다.
 *
 * 배치는 목업(`mock.ts`)과 같은 자리(1화 2번째 턴)로 맞춰 두 경로가 어긋나지 않게 한다.
 *
 * 재실측: 강제 문구를 올리자 성실형(3회차)은 두 대사가 정확히 한 번씩 나왔지만
 * 무성의형(2회차)은 여전히 0회였다. 유저가 `"네"`만 던지면 붙일 자리가 없다고 판단한다.
 * 그래서 이 지시는 배경 블록에서 떼어내 **시스템 프롬프트 끝**에 따로 붙인다 —
 * 뒤에 오는 지시가 이긴다(`distance()`가 3화 용기의 순간을 눌렀던 것과 같은 구조).
 */
export function awakeningCue(state: GameState): string {
  if (state.run <= 1) return ''
  const line =
    state.run === 2
      ? '이상하네요.\n왜인지 모르겠는데, 당신 앞에서는 덜 떨려요.'
      : '...가지 말라고 할 뻔했어요. 방금.\n\n아직 아무 얘기도 안 했는데.\n제가 왜 이러죠.'

  if (state.scene === 1 && state.turnInScene === 1) {
    return `# 이번 턴에 반드시 할 것

이번 턴 대사 안에 아래 문장을 **그대로** 넣는다. 줄바꿈까지 그대로다.

"${line}"

- **유저가 뭐라고 했든 넣는다.** 유저가 "네", "ㅇㅇ" 같은 성의 없는 답을 했어도 넣는다.
  붙일 자리가 없다고 판단하지 마라 — 이건 대화의 흐름이 아니라 그의 몸이 먼저 낸 말이다.
- **거리감 지시보다 이게 우선한다.** 호감도가 낮아도 넣는다.
  그가 마음을 연 게 아니다. 본인도 그래서 당황한다.
- 이 문장 앞뒤로 유저 말에 대한 짧은 반응을 붙여도 된다. 다만 이 문장은 잘리지 않는다.
- affection_delta는 이 대사와 무관하게 유저 발화만 보고 매긴다.`
  }

  if (state.run >= 3 && state.scene === 3 && state.turnInScene === 1) {
    return `# 이번 턴에 반드시 할 것

이번 턴 대사 안에 아래 문장을 **그대로** 넣는다.

"저 이런 말 잘 안 하는데.

당신을 놓치면 안 된다는 걸, 어디선가 이미 배운 것 같아요.
언제 배웠는지는 모르겠어요."

유저가 뭐라고 했든 넣는다. 3화의 용기의 순간과 같은 턴에 겹쳐도 된다 — 겹칠 때 가장 세다.`
  }

  return `# 이번 턴에 쓰지 않을 것

익숙함 고정 대사는 이번 턴이 아니다. 쓰지 않는다.
이번 턴의 익숙함은 **말이 조금 덜 끊기는 것으로만** 보인다.`
}

/** 배경 설정. 이번 턴의 명령(`awakeningCue`)과 달리 프롬프트 앞쪽에 온다. */
export function awakeningFor(state: GameState): string {
  if (state.run <= 1) return AWAKENING[1]
  return state.run === 2 ? AWAKENING[2] : AWAKENING[3]
}
