# 트레이드오프 로그

무엇을 왜 포기했는지의 기록. **append-only** — 기존 행은 수정하지 않는다.

기록 규칙과 근거 쓰는 법은 `/tradeoff-log` 스킬 참조.
포기가 없었으면 트레이드오프가 아니다. 잘한 일을 자랑하는 로그가 아니다.

| # | 결정 | 취한 것 | 버린 것 | 되돌리는 비용 | 근거 |
|---|------|--------|--------|------------|------|
| 1 | 상태를 React 메모리에만 보관 (localStorage도 안 씀) | 하루 절약. 새로고침=재시작으로 컨셉과 오히려 정합 | 이어하기, 기기 간 동기화, 세션 간 지표 누적 | 중 — DB + 세션 키 | 심사자는 한 자리에서 20분 안에 완주한다. 세션 간 영속성이 필요한 사용 패턴이 아니다. [ADR-001](05-decisions/ADR-001-no-persistence.md) |
| 2 | 테스트를 `src/engine/` 순수 로직에만 작성 | 경계값·클램프·폴백 버그를 싸게 차단 | 컴포넌트/API 계층 테스트 커버리지 | 하 — 나중에 추가 가능 | 조용히 틀리고 플레이로 찾으면 비싼 것만 테스트한다. 대사 품질과 delta 분포는 단위 테스트로 못 잡으므로 `/playthrough`가 맡는다 |
| 3 | Next.js 대신 현행 Vite SPA 유지 | 초기 셋업 0시간, 스캐폴드 그대로 사용 | 서버 상태, SSR, Route Handler 편의 | 상 — 재구성 | 서버가 필요한 유일한 이유가 API 키 보호이고 그건 서버리스 함수 1개로 족하다 |
| 4 | 캐릭터 1명 고정 | 프롬프트·대사 검수 대상이 1/N로 줄어 서사 품질에 집중 | 취향 선택, 캐릭터 간 비교 재미 | 하 — 시나리오 복제 | 가설은 "리스크가 애착을 만든다"이지 "선택지가 재미를 만든다"가 아니다. 캐릭터를 늘려도 가설 검증력은 그대로다 |
| 5 | 화별 분기 없음. 엔딩만 3갈래 | 프롬프트 경우의 수가 3화 × 1경로로 고정 | 선택에 따라 상황 자체가 바뀌는 풍부함 | 중 — 화별 프롬프트 분기 추가 | 2~3일에 분기를 넣으면 경우의 수가 급증해 모든 갈래의 대사 품질이 같이 나빠진다. 얕은 4갈래보다 깊은 1갈래가 가설을 더 잘 검증한다 |
| 6 | `affection_delta` 범위를 비대칭(-5 ~ +3)으로 고정 | 아첨 편향에 대한 구조적 방어 | 감정 변화의 세밀한 표현력 | 하 — 상수 하나 | LLM은 순응적으로 훈련돼 delta가 늘 양수로 나온다. 프롬프트 지시만으로는 못 막아서 범위로 강제한다 |
| 7 | 배포는 Vercel에 올리는 것까지만. 인프라 작업 안 함 | 접속 링크 확보에 30분 이하 | 커스텀 도메인, 모니터링, 리전 최적화, CI | 하 | 제출물에 필요한 건 "접속 가능한 링크"이지 운영 가능한 인프라가 아니다 |
| 8 | 의존성 추가: `vitest` (dev) | 상태 엔진 경계값 검증. `pnpm test` 스코프를 `src/engine`으로 제한 | 없음 (devDependency, 번들 미포함) | 하 | 규칙상 의존성 추가는 근거 기록 필수. 엔진 로직은 조용히 틀리고 플레이로 찾으면 비싸다 |
| 9 | 런타임 의존성 추가: `@anthropic-ai/sdk` | 구조화 출력(`output_config.format`)·에러 타입·재시도를 공짜로 얻음. raw fetch로 직접 짤 코드가 사라짐 | 번들 크기 (단, `api/`에서만 import하므로 **클라이언트 번들에는 안 들어간다**) | 하 — fetch로 되돌리면 됨 | 규칙상 런타임 의존성은 근거 필수. JSON 스키마 강제가 파싱 실패율(5% 상한)을 직접 낮추는데, 이걸 손으로 짜는 건 15분 룰에 걸린다 |
| 10 | `vite.config.ts`에 개발용 API 미들웨어 추가 | `pnpm dev` 하나로 배포와 동일한 핸들러가 돈다. `vercel dev` 설치 불필요 | 개발 경로와 프로덕션 경로가 완전히 같지는 않음 (어댑터 한 겹) | 하 — 플러그인 함수 삭제 | 우선순위 1번이 "지금 열어서 대화가 되는가"다. 이게 없으면 로컬에서 대화 자체가 안 된다. 프로덕션 코드에는 관여하지 않는다 |
| 11 | 상태 엔진을 **클라이언트에만** 두고 `api/chat.ts`는 순수 프록시로 | "상태 전이 로직은 한 곳에만"을 물리적으로 보장. 서버는 프롬프트 조립 + 원문 반환만 한다 | 서버가 호감도를 모르므로 서버 측 지표 집계 불가 | 중 — 엔진을 서버로 옮기면 됨 | 두 곳에 두면 반드시 어긋나고, 어긋나면 지표가 전부 무의미해진다. 저장을 안 하므로 서버가 상태를 들 이유도 없다 |
| 12 | 노말엔딩에서도 재도전 허용 (`docs/01`은 배드만 명시) | 익숙함 누적이 끊기지 않음. "조금만 더 했으면"이 곧 재도전 동기 | 노말엔딩의 종결감 | 하 — 조건 한 줄 | 노말에서만 익숙함을 잃게 하면 서사가 어긋난다. 리스크를 줄이는 변경이 아니므로 허용 |
| 13 | 위기 신호를 **모델 경유 없이 코드에서 가로챔** (`looksLikeCrisis`) | 프롬프트가 무시돼도 안전 문구가 100% 나간다. `affection_delta: 0`이 구조적으로 보장됨 | 오탐 시 몰입이 한 번 끊김. 키워드라 우회 가능 | 하 — 패턴 배열 | 안전 항목은 "데모니까 괜찮다"가 성립하지 않는다. 프롬프트 지시만으로는 보장이 아니라 기대일 뿐이다 |
| 14 | `thinking: disabled` + `effort: medium` | 응답 지연 단축. 로맨스에서 3초 넘는 지연은 몰입을 깬다 | 델타 판정의 추론 깊이 | 하 — 파라미터 두 개 | 이 제품에서 모델이 하는 판단(감점 여부)은 얕은 판단이다. 지연이 품질보다 몰입에 더 크게 기여한다. `/playthrough` 델타 분포를 보고 재조정 |
| 15 | 커플 사진 생성(Kaze AI형) 피벗을 거절하고 **트루엔딩 에필로그 고정 스크립트**로 "연인" 프레이밍만 보강 | 반나절 이내에 "연인 구간을 안 보여준다"는 지적을 해소. 기존 M1~M7 전부 유지 | 사진 생성이라는 별개 제품 방향. 에필로그는 읽기 전용이라 상호작용이 없다 | 하 — `EPILOGUE` 상수 + 버튼 하나 | 지적의 전제("썸은 연인이 아니다")는 타당하나 처방이 과했다. 사진 생성기에는 **연인이 없어** 주제(AI 연인 서비스)에서 오히려 멀어지고, Claude API는 이미지를 생성하지 못해 별도 모델·업로드·스토리지가 필요하다 — 하드 제외 3건을 깨고 D3 하루에 재시작하는 비용. 실제 인물 합성은 감수성 항목에서 리스크이기도 하다. 프레이밍 문제는 프레이밍으로 고친다 |
| 16 | `affection_delta` 상한을 +3 → **+4**로, 화당 최소 턴을 6 → **7**로 상향 | 체류 시간 +17%(18→21턴), 매 턴 보상 체감 회복, 첫 시도 트루엔딩 도달 가능 | **트루엔딩 희소성.** 첫 시도에 닿기 어렵게 두려던 설계를 포기했다 | 하 — 상수 두 개 + 테스트 | 실측 근거 둘. (1) 성실한 유저가 1화 내내 `+1`만 받아 "뭘 해도 안 오른다"는 피로가 발생. (2) 시도별 최종 호감도가 70.4/71.3/70.3으로 **재도전 보상이 0**이라, 애초에 "시도를 쌓아 트루에 간다"는 경로가 존재하지 않았다. 리스크는 감점(-5 유지)·배드엔딩·재도전이 지므로 트루 난이도를 낮춰도 가설 검증력은 유지된다 |
| 17 | API 키가 없거나 호출이 실패하면 **규칙 기반 목업 응답**으로 폴백 | 키 없이도 완주·엔딩·재도전 전 경로가 돌아간다. 한도 초과로 데모가 죽지 않는다. `/playthrough`를 **무료로** 돌려 엔진 퇴행을 검증할 수 있다 | **대사가 AI가 아니다.** 서사 품질·캐릭터 일관성은 이 경로로 검증 불가 | 하 — 폴백 분기 삭제 | Claude API 크레딧이 소진돼 실호출이 400을 반환했다. 우선순위 1번이 "지금 열어서 대화가 되는가"인데 그게 무너졌다. 목업은 게임 메커니즘(감점·판별·엔딩 3종 분기)을 그대로 재현하므로 데모와 퇴행 검증에는 충분하고, 대사가 스크립트라는 사실은 **화면 하단과 README에 명시**해 오인을 막는다 |
| 18 | 런타임 LLM을 Claude API → **Gemini 무료 티어**로 교체 | 비용 0으로 "AI 연인 서비스"라는 주제 요구를 다시 충족. 목업(규칙 기반)만으로는 주제에서 벗어난다 | Claude 계열 모델의 대사 품질. 프롬프트를 Gemini 기준으로 재검증해야 함 | 하 — `api/chat.ts` 한 파일 | 과제 요구사항은 "모든 개발은 Claude Code로 진행"이지 런타임 모델 지정이 아니다. 개발 도구와 제품 스택은 별개 축이다. 상태 엔진·프롬프트·UI가 제공자에 독립적이라 교체 비용이 한 파일로 끝났다 — `api/chat.ts`를 순수 프록시로 둔 [#11](#)의 배당금 |
| 19 | `affection_delta`를 `-5~+3`(리스크 우위) → **`-2~+8`(보상 우위)**로 반전. 무성의는 0, 무례는 -2 | 상승 체감과 체류. "적극적으로 표현해도 +1"이라는 실플레이 피로를 해소 | **제품 불변식이었던 "잃기는 쉽고 얻기는 어렵다".** 무례 4번을 적극 1번이 상쇄하므로 턴 단위 리스크는 크게 약해졌다 | 하 — 상수 두 개 | 지시된 배점이다. 리스크를 배점이 지지 않게 된 대신 **배드엔딩·재도전·화별 상한**이 지도록 재배치했다. 비대칭 불변식 테스트는 방향을 뒤집어 다시 고정했다(얻기 > 잃기) — 불변식을 지운 게 아니라 바뀐 것을 명시한다 |
| 20 | 화당 7턴 → **4턴** (총 21턴 → 12턴) + **화별 호감도 천장** 도입 | API 비용 43% 절감. 무료 티어에서 완주가 가능해짐. 유저 피로도 감소 | 화당 대화의 여유. 용기의 순간을 3턴째에 강제로 배치해야 한다 | 하 — 상수 + 시나리오 손질 | 21턴은 Gemini 무료 티어(20요청/일)에서 **한 번도 완주가 안 됐다.** 재도전 제거도 검토됐으나 재도전은 비용을 늘리지 않고 이미 구현돼 있으며, `docs/00`대로 **짧아야 재도전이 가능하다** — 분량 축소는 재도전을 약화시키는 게 아니라 강화한다. 천장은 `+8` 배점에서 1~2화에 트루가 확정되는 것을 막는다 |
| 21 | LLM이 제안한 `affection_delta`에 **유저 발화 기반 상한**을 코드로 씌움 (`classifyEffort` / `effortCeiling`) | 아첨 편향이 배점 설계를 무력화하는 것을 구조적으로 차단. 봇 4종이 트루/노말/배드/배드로 실제로 갈린다 | 감정 판단의 일부가 정규식으로 내려온다. 상한에 걸리는 진심 어린 발화(트리거 없는 긴 고백 등)가 존재할 수 있다 | 하 — 함수 두 개 | 실측에서 Gemini가 `"그래서요?"`에 **+8**, `"몰라요"`에 **+4**를 줬다. 프롬프트에 "무성의는 0"이라 써도 지켜지지 않는다 — 지시는 기대이지 보장이 아니다([#13](#)과 같은 판단). 배점(`-2~+8`)은 그대로 두고 **무엇이 보상받을 자격이 있는지만** 코드가 정한다. 감점은 상한이 막지 않는다 |
| 22 | 제품 개념에서 **타임루프 장르 프레임을 폐기**하고 `3화 완결 호감 누적형 로맨스`로 재규정. 관련 용어를 재도전 / 시도 / 익숙함 누적으로 통일 | 문서와 실제 동작이 일치한다. 카테고리를 정직하게 '누적물'로 놓고 차별점을 **"그 누적을 잃을 수 있는가"** 한 줄로 좁혔다 | **가장 뾰족하다고 적어둔 차별점.** 장르 후킹과 거기서 따온 제품 이름을 버렸다 | 하 — 용어 치환 (동작 변경 없음) | 시도를 건너 이월되는 것은 서툶 레벨 하나뿐이고, 실측에서 그것이 최종 호감도를 못 움직였다(70.4/71.3/70.3). **실제로 일어나지 않는 일을 간판으로 걸고 있었다.** 기존 서비스도 우리도 호감을 누적하며, 다른 것은 배드엔딩에서 그 누적이 초기화된다는 점뿐이다. 메커니즘(`nextRun`·`awkwardness` 100/85/70)은 이탈 방지 완충재로 **그대로 유지**한다 |
| 23 | 런타임 LLM을 Gemini → **Claude(`claude-haiku-4-5`)로 복귀**하고 `@google/genai`를 의존성에서 제거 | 주제(Claude 기반 AI 연인)와 런타임이 일치. 구조화 출력은 `output_config.format`으로 동일하게 강제된다. 제공자 코드가 한 갈래라 디버깅 대상이 줄었다 | **무료 티어라는 안전망.** 이제 크레딧이 마르면 목업으로만 돈다. Gemini 경로는 코드에서 사라졌다(`git show 1c6b572:api/chat.ts`) | 하 — 한 파일 + `pnpm add @google/genai` | [#18](#)은 크레딧 소진에 대한 임시 대응이었고 그 전제가 해소됐다. 교체 비용은 다시 `api/chat.ts` 한 파일 — [#11](#)의 두 번째 배당금. 제공자 스위치(`ANTHROPIC_PROVIDER`)로 둘 다 살리는 안은 **거절**했다: 데모가 죽지 않게 하는 값은 이미 목업 폴백([#17](#))이 지불하고 있고, 한 파일에 두 갈래를 두면 어느 쪽으로 돌았는지 모른 채 지표를 읽게 된다 |
| 24 | Anthropic은 첫 메시지가 `user`여야 하므로 이력 앞쪽의 assistant 턴을 **버린다** | 화의 고정 오프닝(그의 첫 발화)이 있어도 400이 나지 않는다 | 그 화의 오프닝 대사가 모델 컨텍스트에서 빠진다 | 하 — 한 줄 | 오프닝은 `SCENES[n].opening` 고정 문자열이고 같은 내용이 시스템 프롬프트의 화 설명에 이미 들어간다. 유저 메시지로 위장해 끼워넣는 방법도 있으나 그건 **유저 발화가 아닌 것을 유저 발화로 만드는 것**이라 인젝션 취급 규칙(`20-ai-safety.md`)과 충돌한다 |
| 25 | 각성 고정 대사의 **발화 턴을 코드가 정한다** (`awakeningCue`). 지시를 시스템 프롬프트 **끝**으로 옮김 | 익숙함 누적이 화면에서 보인다. 3시도 42턴에서 세 대사가 각각 제 화·제 턴에 **1회씩** 정확히 나왔다 | 대사가 나오는 자리가 고정된다. "자연스러운 자리"라는 재량이 사라졌다 | 하 — 함수 하나 | 실측 3연속. (1) `"1화 초반의 자연스러운 자리에 넣는다"` → 3시도 41턴 동안 **0회.** 모델은 "자연스러우면"을 "안 해도 되면"으로 읽는다. (2) `"반드시 넣는다"`로 올리자 성실형은 나왔으나 무성의형은 여전히 0회 — 유저가 `"네"`만 던지면 붙일 자리가 없다고 판단한다. (3) 같은 지시를 프롬프트 **끝**으로 옮기자 `"네"` 턴에서도 나왔다. **뒤에 오는 지시가 이긴다** — `distance()`가 3화 용기의 순간을 눌렀던 것과 같은 구조다. "진행 제어는 코드, 연기는 LLM"의 적용 범위를 화 전환·엔딩에서 **고정 대사 타이밍까지** 넓힌 것 |
| 26 | 서툶 레벨(100/85/70)의 대사 체감을 **미해결로 남긴다** | D3 남은 시간을 배포·제출에 쓴다. 익숙함 누적의 주된 장치(각성 대사)는 이미 작동한다 | **시도가 쌓일수록 말이 덜 끊긴다**는 설계가 화면에서 증명되지 않는다. `awkwardness()`는 델타 보정으로만 살아 있다 | 중 — 화별 샘플 대사를 레벨별로 3세트씩(3화 × 3레벨 = 9세트) | 실측 턴당 줄바꿈 2.36 / 2.27 / 2.46 — 단조 감소가 아니다. 지시를 `"덜 도망친다"`(추상)에서 `"줄바꿈 2번 이상 / 1~2번 / 1번 이하"`(계량)로 바꿔도 안 움직였다. 원인 가설: **시나리오의 샘플 대사가 few-shot으로 작동해 추상 지시를 이긴다** — 샘플이 전부 줄바꿈 많은 형태다. 고치려면 9세트를 쓰고 감수해야 하는데 15분 룰에 걸린다. 유저가 체감하는 주된 장치는 각성 대사이고 그건 [#25](#)로 해결됐으므로, 보조 장치 하나를 미완으로 두고 넘어간다 |
